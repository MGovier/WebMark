env:
  browser: true
  node: true
  meteor: true

plugins:
  - meteor

extends:
  - airbnb/base
  - plugin:meteor/recommended

rules:
  meteor/audit-argument-checks: 0 # Collection2 package verifies schemas

parserOptions:
  ecmaVersion: 6
  sourceType: "module"
